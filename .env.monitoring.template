# Monitoring Stack Environment Variables
# Copy this file to .env.monitoring and fill in the values

# Environment identifier (dev or prod)
ENVIRONMENT=prod

# Naming
NAME_PREFIX=salko

# Grafana Admin Password
# Add this to GitHub Secrets (development environment) as GRAFANA_ADMIN_PASSWORD
GRAFANA_ADMIN_PASSWORD=changeme

# pgAdmin4 Configuration
# Email and password for pgAdmin4 login
# Add these to GitHub Secrets (development environment)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=changeme

# PostgreSQL Exporter Data Source Names
# Format: postgresql://username:password@hostname:port/database?sslmode=disable
# Note: Both use the same POSTGRES_PASSWORD from GitHub Secrets (development environment)

# Dev database connection
DEV_POSTGRES_EXPORTER_DSN=postgresql://postgres:YOUR_POSTGRES_PASSWORD@salko-postgres-dev:5432/salko?sslmode=disable

# Prod database connection
PROD_POSTGRES_EXPORTER_DSN=postgresql://postgres:YOUR_POSTGRES_PASSWORD@salko-postgres-prod:5432/salko?sslmode=disable
